

import { LinearProgress, LinearProgressProps } from '@mui/material';
import React from 'react';

const UILinearProgress: React.FC<LinearProgressProps> = ({ ...props }) => <LinearProgress { ...props } />

export default UILinearProgress;
