 


import { IconButton, IconButtonProps } from '@mui/material';
import React from 'react';

const UIIconButton: React.FC<IconButtonProps> = ({ ...props }) => <IconButton { ...props } />

export default UIIconButton;
